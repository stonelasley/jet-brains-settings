import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { $PAGECLASS } from './$CLASSFILE';

@NgModule({
    declarations: [
        $PAGECLASS,
        ],
  imports: [
    IonicPageModule.forChild(${PAGECLASS}),
  ],
})
export class ${PAGECLASS}PageModule {}