describe('${TYPE}Service', () => {


	let classUnderTest: ${TYPE}Service;

	beforeEach(() => {

		classUnderTest = new ${TYPE}Service();
	});

	it('should initialize', () => {
	
		expect(classUnderTest).toBeDefined();
	});

});